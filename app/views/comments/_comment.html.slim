- user = comment.user.decorate
.row.comment id="comment_#{comment.id}"
  .col-xs-1
    = user.decorate.avatar_with_link
  .col-xs-9
    .comment_user_name = user.nombres_with_link
    = comment.body
  .col-xs-2.vote_buttons
    = comment.votes
    = comment.vote_buttons
